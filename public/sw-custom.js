// Minimal sw-custom.js for debugging
// This version only logs and does nothing else to avoid blocking workbox

console.log('[SW-Custom] Minimal version loaded - debugging install issue');

// Empty file to test if workbox can install without sw-custom interference
